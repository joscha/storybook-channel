{
  "name": "storybook-channel-mock",
  "description": "Mocks a storybook channel for testing purposes",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "prepublish": "node .scripts/npm-prepublish.js",
    "test": "jest --coverage",
    "tdd": "jest --coverage --watch",
    "semantic-release": "semantic-release pre && npm publish && semantic-release post"
  },
  "license": "MIT",
  "devDependencies": {
    "@kadira/storybook-channel": "^1.1.0",
    "babel-cli": "^6.11.4",
    "babel-jest": "^16.0.0",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-stage-0": "^6.5.0",
    "babel-register": "^6.16.3",
    "chai": "^3.5.0",
    "cz-conventional-changelog": "^1.2.0",
    "jest": "^16.0.2",
    "shelljs": "^0.7.3",
    "sinon": "^1.17.6",
    "semantic-release": "^4.3.5"
  },
  "peerDependencies": {
    "@kadira/storybook-channel": "^1.1.0"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/joscha/storybook-channel-mock.git"
  }
}
